services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dit_ac_control
      POSTGRES_USER: dit_ac
      POSTGRES_PASSWORD: dit_ac_password
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
